import React from "react";
import withLayout from "../../app/navigation/withLayout";

const Pamphlets = () => {
    return (
        <h1>Pamphlets Screen</h1>
    );
};

export default withLayout(Pamphlets);
